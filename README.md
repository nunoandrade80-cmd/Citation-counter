# Citation-counter
A script that queries the Europe PMC API to count citations for a list of articles identified by their PMIDs.
The user will upload a plain text file (.txt) containing the PMIDs. The PMIDs can be either one per line or comma-separated on a single line.
